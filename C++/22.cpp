#include <iostream>
#include <vector>
#include <string>

using namespace std;

class Solution {
public:
    vector<string> generateParenthesis(int n) {
        vector<string> ret;
        if (n == 0) return ret; 
        string templ = "(";
        int lCount = 1;
        while (templ[0] == '(')
        {
            
        }
    }
};

int main()
{
    Solution s;
    return 0;
}